project 'CalTransit.xcodeproj'

# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CalTransit' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CalTransit
  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'MKSpinner'
  
  target 'CalTransitTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CalTransitUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
